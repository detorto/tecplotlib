#include "TecplotZone.h"

TecplotZone::TecplotZone(string name, size_t start_index, string type,
						 size_t dim, size_t i, size_t j, size_t k): name_(name), start_index_(start_index),
						 											type_(type), dim_(dim), i_(i), j_(j), k_(k)
{
	//calculate end index!
}
